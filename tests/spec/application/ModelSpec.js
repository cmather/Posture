define(["Posture/application/Model"], function(Posture) {
  describe("Posture.application.Model", function() {
    it ("should be defined", function() {
      expect(Posture.application.Model).toBeDefined();
    });
    
  });
});